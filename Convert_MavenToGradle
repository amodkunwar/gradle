Convert maven project to gradle in eclipse.

1. Open command prompt
2. gradle init in cmd prompt
3. "gradle clean" in cmd prompt
4. delete pom.xml
5. gradle build in cmd prompt
6. add "apply plugin : 'eclipse'" in build.gradle
7. gradle eclipse in cmd prompt
8. Refresh the project
